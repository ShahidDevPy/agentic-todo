### This file was generated by Nexus Schema
### Do not make changes to this file directly


type DailyBrief {
  overdueCount: Int!
  pendingCount: Int!
  summaryMarkdown: String!

  """
  True when summaryMarkdown was produced by the Gemini API (not the local template fallback).
  """
  usedGemini: Boolean!
}

type Mutation {
  """Deletes all completed todos for this user."""
  clearCompletedTodos(userId: String!): Int!
  createTodo(description: String, dueDateISO: String, priority: TodoPriority, starred: Boolean = false, title: String!, userId: String!): Todo!
  deleteTodo(id: String!, userId: String!): Boolean!

  """Sets sortOrder by array index. Every id must belong to that user."""
  reorderTodos(orderedIds: [String!]!, userId: String!): [Todo!]!
  toggleTodo(id: String!): Todo!

  """
  Partial update. Omit a field to leave it unchanged. Pass nullable fields as null on the wire to clear (title cannot be cleared).
  """
  updateTodo(description: String, dueDateISO: String, id: String!, isCompleted: Boolean, priority: TodoPriority, starred: Boolean, title: String): Todo!
}

type Query {
  """
  LLM-enhanced motivational brief when GEMINI_MODEL + GEMINI_API_KEY are configured; falls back to a deterministic Markdown summary.
  """
  dailyBrief(
    """If true, skip the LLM and use only the deterministic template."""
    deterministicOnly: Boolean = false

    """
    IANA timezone for due-today / overdue bucketing (e.g. America/New_York). Omit or invalid → UTC.
    """
    timeZone: String
    userId: String!
  ): DailyBrief!
  todo(id: String!): Todo

  """
  Tasks for a user. listOrder SMART = high priority & nearest due first; MANUAL = respects sortOrder from drag/reorder.
  """
  todos(
    """ACTIVE = incomplete only (default). COMPLETED = done only. ALL = both."""
    listFilter: TodoListFilter = ACTIVE

    """
    MANUAL = database sortOrder ascending. SMART = priority, due date, then sortOrder.
    """
    listOrder: TodoListOrder = SMART
    starredOnly: Boolean = false
    userId: String!
  ): [Todo!]!
}

type Todo {
  completedAt: String
  createdAt: String!
  description: String
  dueDate: String
  id: String!
  isCompleted: Boolean!
  priority: TodoPriority!
  sortOrder: Int!
  starred: Boolean!
  title: String!
  updatedAt: String!
  userId: String!
}

enum TodoListFilter {
  ACTIVE
  ALL
  COMPLETED
}

enum TodoListOrder {
  MANUAL
  SMART
}

enum TodoPriority {
  high
  low
  medium
}